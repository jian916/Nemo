stages:
  - test

image: debian:unstable

checkfiles:
  stage: test
  script:
  - ./.ci/retry.sh apt-get update
  - ./.ci/retry.sh apt-get -y -qq install ${PACKAGES}
  - ./.ci/checkfiles.sh
  variables:
    PACKAGES: dos2unix moreutils git
